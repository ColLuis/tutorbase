---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/components/auth/LoginForm.tsx
  - src/app/(app)/layout.tsx
  - src/app/(app)/page.tsx
  - src/components/nav/Sidebar.tsx
  - src/components/nav/BottomNav.tsx
autonomous: false
requirements: [AUTH-01, AUTH-03, INFRA-01]

must_haves:
  truths:
    - "Login page renders a form with email and password fields and a submit button"
    - "Login form submits to the login Server Action and shows errors on failure"
    - "App shell has a sidebar visible on md+ screens and hidden on mobile"
    - "App shell has a bottom nav visible on mobile and hidden on md+ screens"
    - "Protected routes redirect to /login when unauthenticated (via middleware)"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Public login page"
      contains: "LoginForm"
    - path: "src/components/auth/LoginForm.tsx"
      provides: "Client-side login form component"
      contains: "use client"
    - path: "src/app/(app)/layout.tsx"
      provides: "Authenticated app shell with responsive nav"
      contains: "Sidebar"
    - path: "src/components/nav/Sidebar.tsx"
      provides: "Desktop sidebar navigation"
      contains: "use client"
    - path: "src/components/nav/BottomNav.tsx"
      provides: "Mobile bottom navigation"
      contains: "use client"
  key_links:
    - from: "src/components/auth/LoginForm.tsx"
      to: "src/lib/actions/auth.ts"
      via: "login Server Action import"
      pattern: "import.*login.*@/lib/actions/auth"
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/nav/Sidebar.tsx"
      via: "Sidebar component import"
      pattern: "import.*Sidebar.*@/components/nav/Sidebar"
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/nav/BottomNav.tsx"
      via: "BottomNav component import"
      pattern: "import.*BottomNav.*@/components/nav/BottomNav"
---

<objective>
Build the login page UI and the authenticated app shell with responsive navigation (sidebar on desktop, bottom nav on mobile).

Purpose: Delivers the visible user-facing experience for Phase 1 -- a tutor can log in and see a protected shell that adapts between mobile and desktop.
Output: Login page with email/password form, app shell layout with sidebar and bottom nav, placeholder home page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
From src/lib/actions/auth.ts:
export async function login(formData: FormData): Promise<{ error: string } | void>
export async function logout(): Promise<never>

From src/components/ui/button.tsx:
shadcn Button component (import { Button } from "@/components/ui/button")

From src/components/ui/input.tsx:
shadcn Input component (import { Input } from "@/components/ui/input")

From src/components/ui/label.tsx:
shadcn Label component (import { Label } from "@/components/ui/label")

From src/components/ui/card.tsx:
shadcn Card components (import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create login page and form component</name>
  <files>
    src/app/(auth)/login/page.tsx,
    src/components/auth/LoginForm.tsx
  </files>
  <read_first>
    src/lib/actions/auth.ts,
    src/components/ui/button.tsx,
    src/components/ui/input.tsx
  </read_first>
  <action>
    1. Create the `(auth)` route group directory: `src/app/(auth)/login/`

    2. Create `src/components/auth/LoginForm.tsx` as a Client Component:
       - Add `'use client'` at the top
       - Import `login` from `@/lib/actions/auth`
       - Import Button from `@/components/ui/button`
       - Import Input from `@/components/ui/input`
       - Import Label from `@/components/ui/label`
       - Use `useActionState` from React (React 19) to handle the Server Action
       - The form has two fields: email (type="email", required) and password (type="password", required)
       - Display error message from the action state if login fails (red text below the form)
       - Show "Signing in..." on the submit button while pending (use `useFormStatus` or the pending state from useActionState)
       - Wrap the login action to work with useActionState signature: `async function loginAction(prevState: any, formData: FormData)`

       Structure:
       ```
       <form action={formAction}>
         <div>
           <Label htmlFor="email">Email</Label>
           <Input id="email" name="email" type="email" required placeholder="jane@example.com" />
         </div>
         <div>
           <Label htmlFor="password">Password</Label>
           <Input id="password" name="password" type="password" required />
         </div>
         {state?.error && <p className="text-sm text-red-500">{state.error}</p>}
         <Button type="submit" className="w-full" disabled={isPending}>
           {isPending ? 'Signing in...' : 'Sign in'}
         </Button>
       </form>
       ```

    3. Create `src/app/(auth)/login/page.tsx` as a Server Component:
       - Import LoginForm from `@/components/auth/LoginForm`
       - Import Card, CardContent, CardHeader, CardTitle from `@/components/ui/card`
       - Center the form vertically and horizontally on the page
       - Use a Card wrapper with title "TutorBase"
       - Add subtitle text "Sign in to manage your tutoring business"

       Layout structure:
       ```
       <div className="min-h-screen flex items-center justify-center px-4">
         <Card className="w-full max-w-sm">
           <CardHeader className="text-center">
             <CardTitle className="text-2xl">TutorBase</CardTitle>
             <p className="text-sm text-muted-foreground">Sign in to manage your tutoring business</p>
           </CardHeader>
           <CardContent>
             <LoginForm />
           </CardContent>
         </Card>
       </div>
       ```

    4. Ensure the (auth) route group does NOT use the (app) layout -- it should use the root layout only.
  </action>
  <verify>
    <automated>cd C:/Users/luisc/Documents/Workspace/tutorbase && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(auth)/login/page.tsx exists
    - src/components/auth/LoginForm.tsx exists
    - LoginForm.tsx contains "'use client'"
    - LoginForm.tsx contains "import { login }" or "import { login" from "@/lib/actions/auth"
    - LoginForm.tsx contains 'name="email"'
    - LoginForm.tsx contains 'name="password"'
    - LoginForm.tsx contains 'type="submit"'
    - LoginForm.tsx contains "text-red" (error display)
    - LoginForm.tsx contains "Signing in" (pending state)
    - login/page.tsx contains "TutorBase"
    - login/page.tsx contains "LoginForm"
    - npm run build exits 0
  </acceptance_criteria>
  <done>Login page renders centered card with email/password form, submits to login Server Action, shows errors and pending state.</done>
</task>

<task type="auto">
  <name>Task 2: Create authenticated app shell with responsive sidebar and bottom nav</name>
  <files>
    src/app/(app)/layout.tsx,
    src/app/(app)/page.tsx,
    src/components/nav/Sidebar.tsx,
    src/components/nav/BottomNav.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md,
    src/lib/actions/auth.ts
  </read_first>
  <action>
    1. Create `src/components/nav/Sidebar.tsx` as a Client Component:
       - Add `'use client'`
       - Import `usePathname` from `next/navigation`
       - Import `logout` from `@/lib/actions/auth`
       - Accept `className` prop (string, optional)
       - Nav items (for Phase 1 placeholders, to be populated in later phases):
         - Home: href="/", icon placeholder (use text or simple SVG)
         - Students: href="/students"
         - Schedule: href="/schedule"
         - Invoices: href="/invoices"
         - Revenue: href="/revenue"
       - Highlight active nav item using `usePathname()` comparison
       - Active state: apply `bg-accent text-accent-foreground` class
       - Inactive state: apply `text-muted-foreground hover:text-foreground hover:bg-accent/50`
       - Include logout button at bottom of sidebar
       - Sidebar width: `w-64`
       - Sidebar should be a `<nav>` element with `fixed left-0 top-0 h-screen` positioning
       - Use `flex flex-col` layout with nav items at top and logout at bottom
       - Each nav item must have min height 44px for touch targets (`min-h-[44px]`)
       - Combine with incoming className prop

    2. Create `src/components/nav/BottomNav.tsx` as a Client Component:
       - Add `'use client'`
       - Import `usePathname` from `next/navigation`
       - Accept `className` prop (string, optional)
       - Same 5 nav items as Sidebar (Home, Students, Schedule, Invoices, Revenue)
       - Use icons or short labels below each item
       - Active state: `text-foreground` class
       - Inactive state: `text-muted-foreground`
       - Add safe area padding: `pb-[env(safe-area-inset-bottom)]`
       - Background with top border: `bg-background border-t`
       - Each nav item min 44x44px tap target
       - Combine with incoming className prop
       - Use `<nav>` element

    3. Create `src/app/(app)/layout.tsx` as a Server Component:
       - Import Sidebar from `@/components/nav/Sidebar`
       - Import BottomNav from `@/components/nav/BottomNav`
       - Structure (matching 01-RESEARCH.md Pattern 5):
         ```
         <div className="min-h-screen">
           <Sidebar className="hidden md:flex" />
           <main className="md:ml-64 pb-16 md:pb-0">
             {children}
           </main>
           <BottomNav className="flex md:hidden fixed bottom-0 left-0 right-0" />
         </div>
         ```

    4. Create `src/app/(app)/page.tsx` as a Server Component placeholder:
       - Simple page with heading "Welcome to TutorBase"
       - Subtitle "Your tutoring management dashboard"
       - This is a placeholder -- the real dashboard comes in Phase 4

    5. Remove or update the default `src/app/page.tsx` that create-next-app generated:
       - If it exists at `src/app/page.tsx` (outside route groups), either delete it or redirect to the (app) route group
       - The root page should be inside `(app)` so it gets the authenticated layout

    6. Verify build passes: `npm run build`
  </action>
  <verify>
    <automated>cd C:/Users/luisc/Documents/Workspace/tutorbase && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/layout.tsx exists
    - src/app/(app)/page.tsx exists
    - src/components/nav/Sidebar.tsx exists
    - src/components/nav/BottomNav.tsx exists
    - Sidebar.tsx contains "'use client'"
    - Sidebar.tsx contains "usePathname"
    - Sidebar.tsx contains "w-64"
    - Sidebar.tsx contains "logout"
    - BottomNav.tsx contains "'use client'"
    - BottomNav.tsx contains "usePathname"
    - BottomNav.tsx contains "safe-area-inset-bottom"
    - layout.tsx contains "hidden md:flex" (sidebar responsive)
    - layout.tsx contains "flex md:hidden" (bottom nav responsive)
    - layout.tsx contains "md:ml-64" (main content offset)
    - layout.tsx contains "pb-16 md:pb-0" (bottom nav padding)
    - page.tsx contains "TutorBase"
    - npm run build exits 0
  </acceptance_criteria>
  <done>App shell renders sidebar on desktop (md+) and bottom nav on mobile, with 5 nav items, active state highlighting, and logout button. Placeholder home page visible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify login flow and responsive navigation</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(app)/layout.tsx</files>
  <action>
    Human verification checkpoint. Run `npm run dev` and visually verify the login page and responsive navigation shell in the browser. No code changes needed -- this confirms Tasks 1 and 2 are working correctly.
  </action>
  <verify>
    <automated>cd C:/Users/luisc/Documents/Workspace/tutorbase && npm run build 2>&1 | tail -3</automated>
  </verify>
  <what-built>Login page with email/password form and authenticated app shell with responsive sidebar (desktop) and bottom nav (mobile)</what-built>
  <how-to-verify>
    1. Run `npm run dev` in the tutorbase directory
    2. Open http://localhost:3000 in browser
    3. Verify you are redirected to /login (middleware should redirect unauthenticated users)
    4. Verify the login page shows a centered card with "TutorBase" title and email/password fields
    5. Try submitting with empty fields -- form validation should prevent submission
    6. Try submitting with invalid credentials -- should show an error message (requires Supabase project to be connected; if not connected, just verify the form submits and you see a connection error, which is expected)
    7. Resize browser to mobile width (< 768px) -- verify bottom nav appears and sidebar disappears
    8. Resize browser to desktop width (>= 768px) -- verify sidebar appears and bottom nav disappears
    9. Check that nav items have adequate tap target size (at least 44px height)
  </how-to-verify>
  <acceptance_criteria>
    - User confirms login page renders correctly
    - User confirms responsive navigation works (sidebar on desktop, bottom nav on mobile)
  </acceptance_criteria>
  <done>User has visually verified the login page and responsive navigation shell work correctly.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Login page renders at /login with email and password fields
- App shell shows sidebar on desktop, bottom nav on mobile
- Middleware redirects unauthenticated users to /login
- Build passes without errors
</verification>

<success_criteria>
- Login page exists at /login with functional form (AUTH-01)
- Middleware protects all routes except /login (AUTH-03)
- Responsive nav: sidebar on md+, bottom nav on mobile (INFRA-01)
- All nav items have 44px+ touch targets
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
