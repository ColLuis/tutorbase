---
phase: 03-invoicing-and-payments
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/invoices/pdf/InvoicePDF.tsx
  - src/components/invoices/pdf/InvoicePDFViewer.tsx
  - src/app/api/invoices/[id]/pdf/route.ts
autonomous: true
requirements: [INV-07]

must_haves:
  truths:
    - "Tutor can download a PDF of an invoice via a GET request to /api/invoices/[id]/pdf"
    - "PDF contains tutor name, invoice number, issue/due dates, bill-to recipient, line items with date/duration/rate/amount, total, and notes"
    - "In-browser PDF preview renders without SSR errors"
  artifacts:
    - path: "src/components/invoices/pdf/InvoicePDF.tsx"
      provides: "@react-pdf Document component for invoice PDF"
      contains: "Document"
    - path: "src/components/invoices/pdf/InvoicePDFViewer.tsx"
      provides: "Client-side PDF preview wrapper with dynamic import"
      contains: "use client"
    - path: "src/app/api/invoices/[id]/pdf/route.ts"
      provides: "Route Handler for PDF download"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/invoices/[id]/pdf/route.ts"
      to: "src/components/invoices/pdf/InvoicePDF.tsx"
      via: "renderToBuffer(<InvoicePDF />)"
      pattern: "renderToBuffer"
    - from: "src/app/api/invoices/[id]/pdf/route.ts"
      to: "src/lib/queries/invoices.ts"
      via: "getInvoice + getTutorForInvoice"
      pattern: "getInvoice"
---

<objective>
Build the PDF generation layer: the InvoicePDF document component, the Route Handler for download, and the client-side PDFViewer for in-browser preview.

Purpose: PDF generation is the highest-risk technical piece in this phase (@react-pdf/renderer in Next.js). Isolating it lets us verify the spike before wiring it into the full UI.
Output: PDF document component, download Route Handler, preview client component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-invoicing-and-payments/03-CONTEXT.md
@.planning/phases/03-invoicing-and-payments/03-RESEARCH.md
@.planning/phases/03-invoicing-and-payments/03-01-SUMMARY.md
@src/lib/queries/invoices.ts
@src/lib/dal.ts
@src/lib/utils/currency.ts
@src/lib/utils/dates.ts

<interfaces>
From src/lib/queries/invoices.ts (created in Plan 01):
```typescript
export async function getInvoice(tutorId: string, invoiceId: string): Promise<InvoiceDetail>
export async function getTutorForInvoice(tutorId: string): Promise<{ name: string }>
```

From src/lib/utils/currency.ts:
```typescript
export function formatCurrency(amount: number, currency?: string): string
```

From src/lib/utils/dates.ts:
```typescript
export function formatShortDate(date: string | Date, timezone: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InvoicePDF document component and Route Handler</name>
  <files>src/components/invoices/pdf/InvoicePDF.tsx, src/app/api/invoices/[id]/pdf/route.ts</files>
  <read_first>src/lib/queries/invoices.ts, src/lib/dal.ts, src/lib/utils/currency.ts, src/lib/utils/dates.ts, next.config.ts</read_first>
  <action>
**Create `src/components/invoices/pdf/InvoicePDF.tsx`** — per D-06 through D-11:

- Import only from `@react-pdf/renderer`: `Document`, `Page`, `Text`, `View`, `StyleSheet`
- NO HTML elements, NO Tailwind — only @react-pdf primitives with `StyleSheet.create()`
- Props: `invoice` object with: tutor_name (string), invoice_number (string), issued_date (string), due_date (string), bill_to (string), items (array of {id, description, quantity, unit_price, amount}), subtotal (number), total (number), notes (string | null)
- Define a TypeScript interface `InvoicePDFData` for these props and export it
- Layout (per D-06, clean text-only, no logo, no branding):
  - Page: A4, padding 40, fontFamily 'Helvetica', fontSize 11
  - Header: tutor_name at fontSize 20 bold (D-07), invoice_number, "Issued: {issued_date}", "Due: {due_date}" (D-11)
  - Bill to section: "Bill to: {bill_to}" (D-08)
  - Table header row: "Description", "Qty", "Rate", "Amount" — bold, border-bottom
  - Line items: one row per item with description, quantity, unit_price formatted as currency, amount formatted as currency (D-09)
  - Total row: right-aligned, bold, "Total: {formatted total}"
  - Notes: if notes exists, render at bottom with some top margin (D-10)
- Format currency as `$X.XX` directly (no Intl.NumberFormat in PDF — use simple `amount.toFixed(2)`)

**Create `src/app/api/invoices/[id]/pdf/route.ts`**:

- Export async GET handler with signature: `GET(_req: Request, { params }: { params: Promise<{ id: string }> })`
- Call `verifySession()` for auth
- Await params, get `id`
- Call `getInvoice(tutorId, id)` — if null, return `new Response('Not found', { status: 404 })`
- Call `getTutorForInvoice(tutorId)` for tutor name
- Build `InvoicePDFData` object: map invoice data, set `bill_to` to `invoice.students.parent_name || invoice.students.name` (D-08)
- Call `renderToBuffer(<InvoicePDF invoice={pdfData} />)` from `@react-pdf/renderer`
- Return `new Response(buffer, { headers: { 'Content-Type': 'application/pdf', 'Content-Disposition': 'attachment; filename="{invoice_number}.pdf"' } })`
  </action>
  <verify>
    <automated>cd C:/Users/luisc/Documents/Workspace/tutorbase && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "Document" src/components/invoices/pdf/InvoicePDF.tsx returns a match
    - grep "StyleSheet" src/components/invoices/pdf/InvoicePDF.tsx returns a match
    - grep "InvoicePDFData" src/components/invoices/pdf/InvoicePDF.tsx returns a match
    - grep "renderToBuffer" src/app/api/invoices/[id]/pdf/route.ts returns a match
    - grep "verifySession" src/app/api/invoices/[id]/pdf/route.ts returns a match
    - grep "application/pdf" src/app/api/invoices/[id]/pdf/route.ts returns a match
    - grep "bill_to" src/components/invoices/pdf/InvoicePDF.tsx returns a match
    - No TypeScript errors in these files (npx tsc --noEmit)
  </acceptance_criteria>
  <done>PDF document component renders tutor name, invoice number, dates, bill-to, line items, total, and notes. Route Handler serves PDF with auth protection.</done>
</task>

<task type="auto">
  <name>Task 2: Create in-browser PDF preview client component</name>
  <files>src/components/invoices/pdf/InvoicePDFViewer.tsx</files>
  <read_first>src/components/invoices/pdf/InvoicePDF.tsx, src/lib/hooks/use-media-query.ts</read_first>
  <action>
**Create `src/components/invoices/pdf/InvoicePDFViewer.tsx`** — per D-12:

- Add `'use client'` directive at top
- Use `dynamic` from `next/dynamic` with `{ ssr: false }` to import `PDFViewer` from `@react-pdf/renderer` — CRITICAL to avoid "window is not defined" SSR crash
- The component accepts the same `InvoicePDFData` props as InvoicePDF
- Import `InvoicePDF` from `./InvoicePDF`
- Dynamic import pattern:
  ```typescript
  const PDFViewerDynamic = dynamic(
    () => import('@react-pdf/renderer').then((mod) => {
      const { PDFViewer } = mod
      return function InvoicePDFViewerInner({ invoice }: { invoice: InvoicePDFData }) {
        return (
          <PDFViewer width="100%" height={600} showToolbar={false}>
            <InvoicePDF invoice={invoice} />
          </PDFViewer>
        )
      }
    }),
    { ssr: false, loading: () => <div className="h-[600px] flex items-center justify-center text-muted-foreground">Loading preview...</div> }
  )
  ```
- Export default component that renders PDFViewerDynamic on desktop (min-width 768px) and a download link fallback on mobile (per research pitfall 5 — PDFViewer iframe height issues on mobile). Use `useMediaQuery` from `@/lib/hooks/use-media-query`.
- Mobile fallback: a link/button pointing to `/api/invoices/{invoiceId}/pdf` with text "Download PDF"
- Accept `invoiceId` prop alongside `invoice` data for the download link
  </action>
  <verify>
    <automated>cd C:/Users/luisc/Documents/Workspace/tutorbase && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "use client" src/components/invoices/pdf/InvoicePDFViewer.tsx returns a match
    - grep "ssr: false" src/components/invoices/pdf/InvoicePDFViewer.tsx returns a match
    - grep "dynamic" src/components/invoices/pdf/InvoicePDFViewer.tsx returns a match
    - grep "useMediaQuery" src/components/invoices/pdf/InvoicePDFViewer.tsx returns a match
    - grep "PDFViewer" src/components/invoices/pdf/InvoicePDFViewer.tsx returns a match
    - No TypeScript errors in this file
  </acceptance_criteria>
  <done>In-browser PDF preview component with SSR-safe dynamic import, desktop iframe preview, and mobile download fallback</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- InvoicePDF renders all required sections per D-06 through D-11
- Route Handler at /api/invoices/[id]/pdf returns application/pdf with auth
- PDFViewer client component uses dynamic import with ssr:false
</verification>

<success_criteria>
- PDF contains: tutor name (no ABN/bank), invoice number, dates, bill-to (parent name fallback), per-lesson line items, total, optional notes
- No SSR errors from @react-pdf/renderer imports
- Route Handler streams PDF as downloadable file
- Mobile users get download link instead of broken iframe
</success_criteria>

<output>
After completion, create `.planning/phases/03-invoicing-and-payments/03-02-SUMMARY.md`
</output>
