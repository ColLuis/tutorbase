---
phase: 03-invoicing-and-payments
plan: "04"
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/app/(app)/invoices/page.tsx
  - src/components/invoices/InvoiceList.tsx
  - src/app/(app)/invoices/[id]/page.tsx
  - src/components/invoices/InvoiceDetail.tsx
  - src/components/invoices/MarkPaidForm.tsx
  - src/components/invoices/DeleteDraftButton.tsx
autonomous: true
requirements: [INV-05, INV-06, INV-07, INV-08, PAY-01, PAY-02]

must_haves:
  truths:
    - "Tutor can view all invoices filtered by status (all, draft, sent, paid, overdue)"
    - "Tutor can view invoice detail with all line items"
    - "Tutor can preview and download invoice PDF from detail page"
    - "Tutor can mark a sent invoice as paid with date and method"
    - "Marking paid auto-generates a receipt visible on detail page"
    - "Tutor can delete a draft invoice with confirmation dialog"
  artifacts:
    - path: "src/app/(app)/invoices/page.tsx"
      provides: "Invoice list page with status filtering"
      contains: "verifySession"
    - path: "src/components/invoices/InvoiceList.tsx"
      provides: "Invoice list component with status tabs"
      contains: "InvoiceList"
    - path: "src/app/(app)/invoices/[id]/page.tsx"
      provides: "Invoice detail page"
      contains: "verifySession"
    - path: "src/components/invoices/InvoiceDetail.tsx"
      provides: "Invoice detail with actions and PDF preview"
      contains: "InvoiceDetail"
    - path: "src/components/invoices/MarkPaidForm.tsx"
      provides: "Inline payment form"
      contains: "markInvoicePaid"
    - path: "src/components/invoices/DeleteDraftButton.tsx"
      provides: "Delete draft with AlertDialog confirmation"
      contains: "AlertDialog"
  key_links:
    - from: "src/components/invoices/InvoiceDetail.tsx"
      to: "src/components/invoices/pdf/InvoicePDFViewer.tsx"
      via: "PDF preview embed"
      pattern: "InvoicePDFViewer"
    - from: "src/components/invoices/MarkPaidForm.tsx"
      to: "src/lib/actions/invoices.ts"
      via: "markInvoicePaid server action"
      pattern: "markInvoicePaid"
    - from: "src/components/invoices/DeleteDraftButton.tsx"
      to: "src/lib/actions/invoices.ts"
      via: "deleteInvoice server action"
      pattern: "deleteInvoice"
---

<objective>
Build the invoice list page (with status filtering), invoice detail page (with PDF preview, mark paid, send, and delete actions), and all supporting components.

Purpose: This wires together all data layer and PDF components into the complete invoice management UI.
Output: Invoice list page, invoice detail page, mark paid form, delete draft button.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-invoicing-and-payments/03-CONTEXT.md
@.planning/phases/03-invoicing-and-payments/03-01-SUMMARY.md
@.planning/phases/03-invoicing-and-payments/03-02-SUMMARY.md
@.planning/phases/03-invoicing-and-payments/03-03-SUMMARY.md
@src/lib/queries/invoices.ts
@src/lib/actions/invoices.ts
@src/components/invoices/pdf/InvoicePDFViewer.tsx
@src/components/invoices/pdf/InvoicePDF.tsx
@src/components/students/DeactivateButton.tsx
@src/components/ui/badge.tsx
@src/components/ui/alert-dialog.tsx

<interfaces>
From src/lib/queries/invoices.ts:
```typescript
export async function getInvoices(tutorId: string, status?: string)
export async function getInvoice(tutorId: string, invoiceId: string)
export async function getTutorForInvoice(tutorId: string)
```

From src/lib/actions/invoices.ts:
```typescript
export async function sendInvoice(formData: FormData)
export async function deleteInvoice(formData: FormData)
export async function markInvoicePaid(formData: FormData)
```

From src/components/invoices/pdf/InvoicePDFViewer.tsx:
```typescript
// Default export — accepts invoice: InvoicePDFData and invoiceId: string
```

From src/components/invoices/pdf/InvoicePDF.tsx:
```typescript
export interface InvoicePDFData { ... }
```

From src/components/students/DeactivateButton.tsx (AlertDialog pattern to follow):
```typescript
// Uses AlertDialog from shadcn for destructive action confirmation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create invoice list page with status filtering</name>
  <files>src/app/(app)/invoices/page.tsx, src/components/invoices/InvoiceList.tsx</files>
  <read_first>src/lib/queries/invoices.ts, src/app/(app)/students/page.tsx, src/components/students/StudentList.tsx, src/components/ui/badge.tsx</read_first>
  <action>
**Create `src/app/(app)/invoices/page.tsx`** — Server Component, per INV-05:
- Call `verifySession()`, read `searchParams` for `status` (default 'all')
- Call `getInvoices(tutorId, status)`
- Render heading "Invoices" and pass data + current status to `InvoiceList`

**Create `src/components/invoices/InvoiceList.tsx`** — `'use client'` component, per D-17 (Claude's discretion on layout):
- Props: `invoices` array, `currentStatus` string
- **Status filter tabs**: horizontal scrollable row of buttons/links for: All, Draft, Sent, Paid, Overdue. Each links to `/invoices?status=X`. Highlight active tab. Use `useRouter` and `useSearchParams`.
- **Invoice rows**: Card-based layout (carry forward from student list pattern). Each card shows:
  - Invoice number (e.g. "INV-0001") and student name
  - Status badge: draft (gray), sent (blue), paid (green), overdue (red) — use shadcn Badge with appropriate variant. Compute overdue: status === 'sent' AND due_date < today.
  - Issue date and total amount (formatted with formatCurrency)
  - Tap entire card to navigate to `/invoices/{id}`
- **Empty state**: "No invoices yet. Create one from a student's page." with link to `/students`
- Mobile-first: single column, 44px tap targets, card min-height for comfortable tapping
  </action>
  <verify>
    <automated>cd C:/Users/luisc/Documents/Workspace/tutorbase && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "verifySession" src/app/(app)/invoices/page.tsx returns a match
    - grep "getInvoices" src/app/(app)/invoices/page.tsx returns a match
    - grep "use client" src/components/invoices/InvoiceList.tsx returns a match
    - grep "status" src/components/invoices/InvoiceList.tsx returns a match (filter tabs)
    - grep "Badge" src/components/invoices/InvoiceList.tsx returns a match
    - grep "No invoices" src/components/invoices/InvoiceList.tsx returns a match (empty state)
    - File src/app/(app)/invoices/page.tsx exists
  </acceptance_criteria>
  <done>Invoice list page shows all invoices with status filter tabs (all/draft/sent/paid/overdue), badge indicators, and card-based layout</done>
</task>

<task type="auto">
  <name>Task 2: Create invoice detail page with PDF preview, mark paid, send, and delete actions</name>
  <files>src/app/(app)/invoices/[id]/page.tsx, src/components/invoices/InvoiceDetail.tsx, src/components/invoices/MarkPaidForm.tsx, src/components/invoices/DeleteDraftButton.tsx</files>
  <read_first>src/lib/queries/invoices.ts, src/lib/actions/invoices.ts, src/components/invoices/pdf/InvoicePDFViewer.tsx, src/components/invoices/pdf/InvoicePDF.tsx, src/components/students/DeactivateButton.tsx, src/components/ui/alert-dialog.tsx</read_first>
  <action>
**Create `src/app/(app)/invoices/[id]/page.tsx`** — Server Component, per INV-06:
- Call `verifySession()`, await params for `id`
- Call `getInvoice(tutorId, id)` and `getTutorForInvoice(tutorId)` — if no invoice, call `notFound()`
- Build `InvoicePDFData` for the viewer from invoice + tutor data
- Pass all data to `InvoiceDetail` client component

**Create `src/components/invoices/InvoiceDetail.tsx`** — `'use client'`, per INV-06, D-12 through D-16:
- Props: invoice data (with items, receipt, student info), pdfData (InvoicePDFData), invoiceId
- **Header**: invoice_number, status badge, back link to /invoices
- **Summary section**: student name, issued date, due date, total (formatted)
- **Line items table**: description, qty, rate, amount for each item — responsive table that stacks on mobile
- **Notes**: display if present
- **PDF Preview section** (D-12): embed `InvoicePDFViewer` component, plus a "Download PDF" link to `/api/invoices/{id}/pdf`
- **Action buttons** (conditionally shown by status):
  - Draft: "Send Invoice" button (calls sendInvoice), "Delete" button (DeleteDraftButton), "Download PDF" link
  - Sent: "Mark as Paid" button (shows MarkPaidForm), "Download PDF" link
  - Paid: "Paid on {date} via {method}" status line (D-16), receipt info (D-15), "Download PDF" link
- **Receipt display** (D-15): when invoice is paid and receipt exists, show "Receipt {receipt_number} — Paid {formatted_date}" below the status line

**Create `src/components/invoices/MarkPaidForm.tsx`** — `'use client'`, per D-13, D-14, PAY-01, PAY-02:
- Props: invoiceId (string)
- Inline form (not a modal): payment date input (type="date", defaults to today) and payment method select (options: "Bank Transfer", "Cash", "Card", "Other" — or free text input)
- Submit calls `markInvoicePaid` server action
- On success: toast "Payment recorded" (D-14), revalidation handles UI update
- On error: toast error message
- Use `useTransition` for pending state
- Compact layout — appears inline below the "Mark as Paid" button when toggled

**Create `src/components/invoices/DeleteDraftButton.tsx`** — per INV-08, D-17:
- Follow exact pattern from `DeactivateButton.tsx` (AlertDialog confirmation)
- AlertDialog: title "Delete Invoice?", description "This will permanently delete this draft invoice. Lessons will become available for future invoices."
- On confirm: call `deleteInvoice` server action, redirect to `/invoices` on success, toast on error
  </action>
  <verify>
    <automated>cd C:/Users/luisc/Documents/Workspace/tutorbase && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "verifySession" src/app/(app)/invoices/[id]/page.tsx returns a match
    - grep "getInvoice" src/app/(app)/invoices/[id]/page.tsx returns a match
    - grep "InvoicePDFViewer" src/components/invoices/InvoiceDetail.tsx returns a match
    - grep "markInvoicePaid" src/components/invoices/MarkPaidForm.tsx returns a match
    - grep "Payment recorded" src/components/invoices/MarkPaidForm.tsx returns a match
    - grep "AlertDialog" src/components/invoices/DeleteDraftButton.tsx returns a match
    - grep "deleteInvoice" src/components/invoices/DeleteDraftButton.tsx returns a match
    - grep "Paid on" src/components/invoices/InvoiceDetail.tsx returns a match (D-16 status line)
    - grep "receipt_number" src/components/invoices/InvoiceDetail.tsx returns a match (D-15)
    - grep "sendInvoice" src/components/invoices/InvoiceDetail.tsx returns a match
    - File src/app/(app)/invoices/[id]/page.tsx exists
    - npx tsc --noEmit exits cleanly
  </acceptance_criteria>
  <done>Invoice detail page shows full invoice with line items, PDF preview/download, conditional action buttons (send/delete for draft, mark paid for sent, receipt for paid), and all payment recording with auto-receipt generation</done>
</task>

</tasks>

<verification>
- Invoice list page at /invoices shows invoices with status tabs
- Invoice detail at /invoices/[id] shows line items, PDF preview, and appropriate action buttons
- Mark paid form records payment and creates receipt
- Delete draft uses AlertDialog confirmation and redirects
- Send action changes status from draft to sent
- Paid invoices show receipt info and "Paid on X via Y" line
</verification>

<success_criteria>
- INV-05: status filter tabs work for all/draft/sent/paid/overdue
- INV-06: detail page shows all line items
- INV-07: PDF preview embedded on detail, download link works
- INV-08: delete draft with AlertDialog confirmation
- PAY-01: mark paid with date and method
- PAY-02: receipt auto-generated and visible on detail
- D-13: inline form for payment
- D-15: receipt visible on detail, no PDF
- D-16: "Paid on [date] via [method]" status line
</success_criteria>

<output>
After completion, create `.planning/phases/03-invoicing-and-payments/03-04-SUMMARY.md`
</output>
