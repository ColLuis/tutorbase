---
phase: 03-invoicing-and-payments
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(app)/invoices/new/page.tsx
  - src/components/invoices/CreateInvoiceForm.tsx
  - src/app/(app)/students/[id]/page.tsx
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04]

must_haves:
  truths:
    - "Tutor can navigate from student detail to create invoice page"
    - "Create invoice page shows un-invoiced completed lessons with checkboxes"
    - "Tutor can set issue date, due date, and notes"
    - "Tutor can save as draft or finalise as sent"
  artifacts:
    - path: "src/app/(app)/invoices/new/page.tsx"
      provides: "Server component for create invoice page"
      contains: "verifySession"
    - path: "src/components/invoices/CreateInvoiceForm.tsx"
      provides: "Client component with lesson selection and invoice form"
      contains: "use client"
  key_links:
    - from: "src/app/(app)/students/[id]/page.tsx"
      to: "/invoices/new?studentId="
      via: "Link component"
      pattern: "invoices/new"
    - from: "src/components/invoices/CreateInvoiceForm.tsx"
      to: "src/lib/actions/invoices.ts"
      via: "createInvoice server action"
      pattern: "createInvoice"
---

<objective>
Build the invoice creation flow: "Create Invoice" button on student detail page, full-page form with lesson selection, date fields, and save/send actions.

Purpose: This is the primary entry point for invoice creation (D-01) and covers the core invoice creation requirements.
Output: Create invoice page, form component, updated student detail page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-invoicing-and-payments/03-CONTEXT.md
@.planning/phases/03-invoicing-and-payments/03-01-SUMMARY.md
@src/lib/actions/invoices.ts
@src/lib/queries/invoices.ts
@src/app/(app)/students/[id]/page.tsx
@src/lib/utils/currency.ts
@src/lib/utils/dates.ts

<interfaces>
From src/lib/queries/invoices.ts (Plan 01):
```typescript
export async function getUnInvoicedLessons(tutorId: string, studentId: string): Promise<Lesson[]>
```

From src/lib/actions/invoices.ts (Plan 01):
```typescript
export async function createInvoice(formData: FormData): Promise<{ success: true; invoiceId: string } | { error: string }>
```

From src/lib/queries/students.ts:
```typescript
export async function getStudent(tutorId: string, studentId: string): Promise<Student>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Create Invoice" button to student detail and build create invoice page</name>
  <files>src/app/(app)/students/[id]/page.tsx, src/app/(app)/invoices/new/page.tsx, src/components/invoices/CreateInvoiceForm.tsx</files>
  <read_first>src/app/(app)/students/[id]/page.tsx, src/lib/queries/invoices.ts, src/lib/actions/invoices.ts, src/lib/queries/students.ts, src/lib/utils/currency.ts, src/lib/utils/dates.ts, src/components/students/StudentForm.tsx, src/components/schedule/LessonDrawer.tsx</read_first>
  <action>
**Update `src/app/(app)/students/[id]/page.tsx`** — per D-01:
- Add a "Create Invoice" button (Link styled as Button) in the action buttons area alongside Edit and Deactivate
- Link to `/invoices/new?studentId=${student.id}`
- Only show if student has is_active = true (can't invoice inactive students with no new lessons)

**Create `src/app/(app)/invoices/new/page.tsx`** — Server Component:
- Read `searchParams` for `studentId` — if missing, redirect to `/students`
- Call `verifySession()` then `getStudent(tutorId, studentId)` and `getUnInvoicedLessons(tutorId, studentId)`
- Query tutor timezone from tutors table (for date formatting in the form)
- Render page with heading "New Invoice for {student.name}", back link to `/students/{studentId}`
- Pass lessons, student, and timezone to `CreateInvoiceForm` client component
- If no un-invoiced lessons, show empty state: "No completed lessons to invoice" with link back

**Create `src/components/invoices/CreateInvoiceForm.tsx`** — `'use client'` component, per D-02 through D-05:
- Props: `lessons` (un-invoiced completed lessons), `studentId`, `studentName`, `timezone`
- **Lesson selection** (D-03): render each lesson as a checkbox row showing: formatted date, duration (e.g. "60 min"), rate (formatted currency), calculated amount. All checked by default. Tutor can uncheck lessons to exclude.
- **Running total**: compute and display subtotal based on selected lessons
- **Date fields** (D-04, INV-03): issue date (defaults to today, date input type="date") and due date (defaults to 14 days from today, date input type="date")
- **Notes field** (D-10): textarea, optional, placeholder "Payment due within 14 days"
- **Action buttons** (D-05, INV-04):
  - "Save Draft" button — submits with status='draft'
  - "Send Invoice" button — submits with status='sent'
  - Both call `createInvoice` server action via FormData
  - Include hidden fields: studentId, selected lessonIds (comma-separated), issuedDate, dueDate, notes, status
- On success: `router.push('/invoices/' + invoiceId)` using returned invoiceId
- On error: show error via toast (sonner)
- Use `useTransition` for pending state on buttons
- Mobile-first layout: single column, 44px minimum tap targets
  </action>
  <verify>
    <automated>cd C:/Users/luisc/Documents/Workspace/tutorbase && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "invoices/new" src/app/(app)/students/[id]/page.tsx returns a match (Create Invoice link)
    - grep "use client" src/components/invoices/CreateInvoiceForm.tsx returns a match
    - grep "createInvoice" src/components/invoices/CreateInvoiceForm.tsx returns a match
    - grep "Save Draft" src/components/invoices/CreateInvoiceForm.tsx returns a match
    - grep "checkbox" src/components/invoices/CreateInvoiceForm.tsx returns a match (lesson selection)
    - grep "verifySession" src/app/(app)/invoices/new/page.tsx returns a match
    - grep "getUnInvoicedLessons" src/app/(app)/invoices/new/page.tsx returns a match
    - File src/app/(app)/invoices/new/page.tsx exists
    - npx tsc --noEmit exits cleanly
  </acceptance_criteria>
  <done>Tutor can navigate from student detail to full-page invoice creation form, select lessons with checkboxes, set dates and notes, and save as draft or send</done>
</task>

</tasks>

<verification>
- Student detail page has "Create Invoice" link to /invoices/new?studentId=X
- Create invoice page loads un-invoiced lessons for the student
- Lessons shown with checkboxes, all selected by default
- Issue date defaults to today, due date to today + 14 days
- Save Draft and Send buttons both work
- Empty state shown when no un-invoiced lessons exist
</verification>

<success_criteria>
- Per D-01: entry point is student detail page
- Per D-02: full-page form, not drawer
- Per D-03: checkbox lesson selection
- Per D-04: draft preview with dates, items, total
- Per D-05: save creates draft, send finalises
</success_criteria>

<output>
After completion, create `.planning/phases/03-invoicing-and-payments/03-03-SUMMARY.md`
</output>
