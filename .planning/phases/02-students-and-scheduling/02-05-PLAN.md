---
phase: 02-students-and-scheduling
plan: 05
type: execute
wave: 4
depends_on: [02-03, 02-04]
files_modified: []
autonomous: false
requirements: [STUD-02, STUD-03, STUD-04, STUD-05, SCHED-03, SCHED-04, SCHED-05, SCHED-06]

must_haves:
  truths:
    - "Student list shows active students with search working"
    - "Adding a student persists it to the database and it appears in the list"
    - "Editing a student updates the record visible on the detail page"
    - "Deactivating a student hides them from the active list (preserved in inactive view)"
    - "Schedule shows current week lessons in both calendar and list views"
    - "Marking a lesson complete shows a toast and updates its color"
    - "Lesson form opens as bottom drawer on mobile, side sheet on desktop"
    - "Recurring lessons create multiple entries on the schedule"
  artifacts:
    - path: "src/app/(app)/students/page.tsx"
      provides: "Functional student list page"
    - path: "src/app/(app)/schedule/page.tsx"
      provides: "Functional schedule page with dual views"
  key_links:
    - from: "browser at /students"
      to: "Supabase students table"
      via: "Student list renders data from DB via getStudents query"
      pattern: "manual-verify"
    - from: "browser at /schedule"
      to: "Supabase lessons table"
      via: "Calendar/list renders lessons from DB via getLessonsForWeek query"
      pattern: "manual-verify"
---

<objective>
Visual and functional verification of all Phase 2 features against a running dev server connected to Supabase.

Purpose: Code that builds is not the same as features that work. This checkpoint confirms the end-to-end flow functions as designed — from the browser to the database and back — before Phase 2 is marked complete.

Output: Human confirms each success criterion below, or reports issues for gap closure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-students-and-scheduling/02-03-SUMMARY.md
@.planning/phases/02-students-and-scheduling/02-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 2 student and schedule features end-to-end</name>
  <files>src/app/(app)/students/page.tsx, src/app/(app)/schedule/page.tsx</files>
  <action>
    Human verification checkpoint. Apply the schema migration, re-run the seed script, start the dev server, and verify all Phase 2 features work correctly in the browser. No code changes needed — this confirms Plans 02-01 through 02-04 are working correctly.
  </action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint requiring live browser testing</automated>
  </verify>
  <what-built>
    Complete Phase 2: Students and Scheduling features

    Plans 01-04 delivered:
    - Schema migration (subject + default_duration_minutes columns)
    - Data layer: queries and server actions for students, lessons, and tutor profile
    - Student CRUD pages: list, add, view, edit, deactivate (/students/*)
    - Tutor profile edit (/profile)
    - Schedule page with calendar + list views, lesson drawer, and status quick actions (/schedule)
  </what-built>
  <how-to-verify>
    Before starting — apply the schema migration and re-run the seed script:

    Apply migration in Supabase Dashboard > SQL Editor:
    Run contents of supabase/migrations/002_students_subject_duration.sql

    Then update seed data:
    npm run seed

    Start the dev server: npm run dev
    Open http://localhost:3000
    Login: jane@example.com / password123

    --- Students (STUD-01 through STUD-05) ---

    1. Navigate to /students
       - Active students list is visible (Alice Smith, Bob Jones)
       - Charlie Brown (inactive) is NOT visible by default
       - Search input filters students by name as you type
       - "Show inactive" toggle reveals Charlie Brown with "Inactive" badge

    2. Click "Add Student"
       - Form opens at /students/new with all fields: name, parent contact, subject, rate, duration, notes
       - Fill in a new student and submit
       - New student appears in the /students list after redirect

    3. Click on Alice Smith
       - Detail page shows name, subject (Mathematics), parent info, rate ($75.00/hr), duration (60 min)
       - "Edit" button is present

    4. Click "Edit" on Alice Smith
       - Edit form pre-fills all existing values
       - Change subject to "Maths and Science" and save
       - Detail page shows updated subject

    5. Test deactivation with the new test student (not Alice/Bob):
       - "Deactivate Student" button triggers a confirmation dialog (not window.confirm)
       - After confirming, student disappears from active list
       - Visible in list when "Show inactive" is toggled on

    --- Schedule (SCHED-01 through SCHED-07) ---

    6. Navigate to /schedule
       - Week calendar grid is visible with correct days (Mon-Sun)
       - Alice's upcoming lesson appears as a colored block on the correct day
       - Week nav arrows navigate to previous/next weeks

    7. Toggle to list view
       - Switches instantly (no page reload, same URL base)
       - Lessons grouped by day with day headers

    8. Click a lesson block
       - Detail panel opens showing student name, date/time, duration, rate
       - "Complete", "Cancel", "No-show" buttons are visible

    9. Mark Alice's future lesson as Complete
       - No confirmation dialog appears (happy path, D-10)
       - Toast notification "Lesson marked complete" appears at bottom (D-11)
       - Lesson block color changes to green

    10. Click "+ Lesson" button
        - On mobile: bottom drawer slides up
        - On desktop: side sheet slides in from the right
        - Student combobox is searchable (type to filter names)
        - Selecting a student auto-fills rate and duration from their defaults (SCHED-07)
        - Date picker works, time field accepts free text ("3:45", "14:00", "2:15 pm")

    11. Create a recurring lesson
        - Enable "Repeat weekly" toggle — N weeks input appears
        - Submit with 3 weeks
        - 3 lessons appear on the schedule spread across 3 consecutive weeks

    12. Navigate to /profile
        - Name and email are pre-filled
        - Changing name and saving updates the profile

    13. On a narrow viewport (or browser DevTools mobile emulation):
        - Bottom nav is visible at the bottom (not sidebar)
        - Lesson drawer opens from the bottom (not a side sheet)
        - Buttons are tap-friendly (visually large enough)
  </how-to-verify>
  <acceptance_criteria>
    - User confirms student list renders active students with working search and inactive toggle
    - User confirms student add/edit/deactivate flows work end-to-end
    - User confirms schedule calendar and list views render with correct lessons
    - User confirms marking complete shows toast (no dialog) per D-10/D-11
    - User confirms lesson drawer is bottom drawer on mobile, side sheet on desktop per D-04
    - User confirms student defaults pre-fill on combobox select per SCHED-07
    - User confirms recurring lessons create N entries spread across N weeks per SCHED-02
  </acceptance_criteria>
  <done>Human has confirmed all Phase 2 success criteria against a live dev server. Phase 2 is complete.</done>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe any failures: e.g. "calendar not rendering", "toast not appearing", "form validation error on recurring lesson".
    Failures will be used to create gap closure plans.
  </resume-signal>
</task>

</tasks>

<verification>
Human has confirmed all Phase 2 success criteria against a live dev server.
</verification>

<success_criteria>
All 13 verification groups pass. Human types "approved". Phase 2 can be marked complete in ROADMAP.md.
</success_criteria>

<output>
After human approval:
1. Update ROADMAP.md Phase 2 status to complete with today's date
2. Update STATE.md current position to Phase 3
3. Create `.planning/phases/02-students-and-scheduling/02-05-SUMMARY.md`
</output>
